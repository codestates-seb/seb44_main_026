import { styled } from 'styled-components';
const StyledHeaderContainer = styled.div`
  display: flex;
  background-color: var(--white);
`;

export const Header = () => {
  return (
    <div>
      <StyledHeaderContainer>aaa</StyledHeaderContainer>
    </div>
  );
};
